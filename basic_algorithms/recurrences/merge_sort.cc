#include <cstdio>
#include <cstdlib>

int merge(int a[], int b[])
{

}

int merge_sort(int vals[], len)
{
	if (len == 1)
		return vals;
	int mid = len/2;

}

int main()
{
	int len = 11;
 	int unsorted[] = {93, 12, 23, 45, 12, 1, 0, 18, 33, 56, 73};	
	int sorted[len];
	sorted = merge_sort(unsorted, len);
	return EXIT_SUCCESS;
}
